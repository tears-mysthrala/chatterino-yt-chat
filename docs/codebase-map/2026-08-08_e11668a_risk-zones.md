# Codebase Map - Risk Zones

Snapshot: `e11668a03c75cf4ed8b3ef8c8d071bd0c2f34592` on `main`.

| Zone or module | Files involved | Risk reason | Potential impact | Tests to run | Manual checks | Future warning signs |
|---|---|---|---|---|---|---|
| URL/network trust boundary | `src/youtube/url.lua`, `src/support/validation.lua`, `src/c2_adapter.lua`, `.github/workflows/ci.yml` | User URLs and upstream-derived URLs can influence requests | SSRF, unexpected hosts, privacy leakage | URL, support, fuzz, integration; CI allowed-host check | Inspect every new request construction and redirect path | New host literals, relaxed scheme/host parsing, redirects followed implicitly |
| Innertube compatibility | `src/youtube/html.lua`, `innertube.lua`, `continuations.lua`, `actions.lua`, `renderers.lua` | Relies on undocumented and changing YouTube shapes | Silent chat loss, tight polling, broken streams | unit action/renderer and continuation suites; all fixtures; fuzz | Test a live and offline channel in supported Chatterino | Unknown-event spikes, missing continuations, HTML extraction failures |
| Polling state machine | `src/youtube/polling.lua`, `state/active_streams.lua`, `support/backoff.lua`, `support/clock.lua` | Async callbacks share mutable stream/split state | Duplicate polls/messages, runaway retries, leaks, stalled chat | integration harness, state, support, load | Open/close multiple splits and reconnect after errors | Increasing timer count, stale streams, duplicate delivery, retry storms |
| Persistence and migrations | `src/state/persistence.lua`, `migrations.lua`, `channels.lua` | Writes occur in a restricted sandbox without guaranteed atomic rename | Lost bindings/settings or unrecoverable corrupt state | persistence and state suites, integration harness | Interrupt writes; test corrupt primary with valid/invalid backup | Schema bump without migration, fields outside validation, backup overwritten too early |
| Host API boundary | `src/c2_adapter.lua`, `src/init.lua`, `src/commands.lua` | Chatterino API capabilities/version differences affect all IO and delivery | Plugin fails to load or messages cannot render/update | c2 mock and integration harness | Install in claimed minimum Chatterino version | New direct `c2` calls outside adapter, mock/API divergence |
| Hostile payload rendering | `src/messages/**`, `src/youtube/actions.lua`, `renderers.lua`, `support/validation.lua` | Chat content and future schemas are attacker-controlled | Exceptions, UI flooding, deceptive output, excessive memory | actions/renderers, fuzz, fixtures, load | Inspect truncation and fallback display with malformed payloads | Unbounded recursion/arrays/strings, handler without `pcall`/fallback |
| Logging and secrets | `src/support/logging.lua`, `polling.lua`, `commands.lua` | Requests include API keys/continuations and payloads include user content | Credential/token or privacy leakage | support and fuzz tests; CI secret scan | Run debug mode and inspect logs from failures/unknown renderers | Raw payload logging, sensitive fields added without redaction |
| Monetary and membership semantics | `src/messages/monetary.lua`, `memberships.lua`, `builder.lua` | Values are display-sensitive and upstream-localized | Misleading payment/membership representation | actions/renderers plus monetary fixtures | Compare against current YouTube UI for representative locales | Numeric conversion, currency inference, missing verbatim amount |
| Supply chain and release | `libs/json.lua`, `.github/workflows/*.yml`, `scripts/build_release.sh`, `scripts/sha256.sh` | Vendored parser and release workflow define shipped artifact | Compromised dependency or non-reproducible package | full CI, reproducible build, checksum generation | Review pinned actions and ZIP contents before release | Unpinned actions, new opaque binary, artifact content drift |

## Current disposition

**Observed in code:** Local test and fixture suites pass at this snapshot.

**Pending verification:** Windows PATH lacks direct Lua and luacheck, so lint and direct interpreter version checks were not completed in this session. Live Chatterino/YouTube behavior and full release reproducibility also remain manual or CI verification items.

**Risk if modified:** Changes to `polling.lua`, `c2_adapter.lua`, `validation.lua`, persistence/migrations, or the action/renderer dispatch tables should receive full-suite, fuzz/load, and targeted manual validation before release.
